version: "3.1"

rules:

- rule: Rispondi al meteo se slot impostati
  condition:
    - slot_was_set:
        - gp
        - year
  steps:
    - intent: ask_weather_info
    - action: action_get_weather_info
